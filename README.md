# Vocab Electron App (MVP)

Commands:

```bash
npm install
npm run dev
```

This starts Vite dev server and launches Electron. The app stores CSV files under Electron `app.getPath('userData')/vocab-data/`.
